import { Header } from "../common/Header";
import Table from "../common/Table";
import Layout from "../common/Layout";

const Home = () => {
  return (
    <Layout>
      <Header />
      <Table />
    </Layout>
  );
};

export default Home;
